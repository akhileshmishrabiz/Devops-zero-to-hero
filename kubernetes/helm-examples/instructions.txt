# Create a directory called "mychart"
mkdir mychart  

# Change directory to "mychart"
cd mychart  

# Create a Helm chart named "mychart"
helm create mychart  
helm install mychart ./mychart  # Install the Helm chart named "mychart"
kubectl get pods  # Get the list of pods in the Kubernetes cluster
kubectl get services  # Get the list of services in the Kubernetes cluster
